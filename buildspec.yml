version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo Build started on `date`
      - echo 'All tests passed (placeholder)'
artifacts:
  files:
    - '**/*'